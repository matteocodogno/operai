// Test-only resolution stub for the `admin/version` federated module
// (version-bump plan §C). See estimaiVersion.ts in this directory for the
// full rationale.
export const REMOTE_VERSION = '0.0.0-stub'
