---
spec: 006
evaluated: 2026-07-14
rubric: default-v2
score: 95
verdict: PASS
---
# Eval report: User invitations, resend, and user deletion

| Dimension | Weight | Score (/5) | Floor | Weighted | Evidence |
|---|---|---|---|---|---|
| AC satisfaction | 35 | 5 | 4 | 35.0 | All 45 ACs (US-1..US-6) mapped in `tasks.md` coverage map + `plan.md` test table, each to ≥1 passing test AND observable behavior. Verified live: auth `bun test` **293 pass / 905 expects** (`invitations.routes.test.ts`, `invite.routes.test.ts`, soft-delete in `users.routes.ts`, hooks in `auth.config.test.ts` incl. the R1 abort-contract spike), notify-api **106 pass**, admin-ui **257 pass**. Live `shell/e2e/invitations.spec.ts` **5/5** end-to-end: invite→accept→exactly-invited-role/no-baseline (`:76`), landing+resend-invalidates-old-link (`:167`), delete confirm→vanish→re-sign-in refused (`:205`), bulk delete both/zero-skip (`:257`), server-side self-delete refusal (`:293`). The QE-found AC-2.3 defect (exact-roles) is fixed and re-verified in code and e2e. |
| Spec fidelity | 20 | 5 | 3 | 20.0 | Implementation matches plan intent exactly: two better-auth seams (`user.create.after` + `session.create.before`, `auth.config.ts:325-343`), notify-api email as a 2nd channel (`notify-api/src/channels/`, `system/emails.routes.ts`), soft-delete + synchronous `session.deleteMany` (`users.routes.ts:543-547`), derived-`expired` state — all as `plan.md` §Architecture. Zero unrequested product scope. The AC-2.3 R9 ambiguity was resolved by the QE loop toward the STRICT spec reading ("no more, no fewer" ⇒ named-role invite REPLACES baseline `employee`; empty invite keeps it) — `auth.config.ts:110-124` — which is the spec's own wording, not drift. The OWASP hardening (commit `e8cb3b5`) was scheduled by `plan.md` §Security (frontier tier), so requested not creep. `design.md` §Gaps routes AC-2.4 wrong-account + landing-bilingual to PO rather than silently scoping them. |
| Test quality | 20 | 5 | 3 | 20.0 | Tests verify each AC plus edge/error cases and defeat vacuous passes. e2e asserts the exact QE defect as a control (active user holds ONLY the invited role, no baseline `employee`, `:76`), resend-invalidates-OLD-link, blocked re-sign-in, and a server-side self-delete refusal that is defense-in-depth BEYOND the disabled UI control (`:293`). R1's `session.create.before` abort contract ships a permanent empirical test, not just a code comment. Integration covers 409 active-user/live-pending, the P2002 concurrent-create race→409, 422 accepted/revoked resend+revoke, non-admin→403 across every mutating route, email-failure-still-201, and the FK-existence-skip path (role deleted mid-window). |
| Code quality | 15 | 5 | 3 | 15.0 | Idiomatic clones of the existing auth/notify-api stack: `withAudit` transactions, Prisma-only DB access, Hono/zod-openapi routes, RFC 7807. No hallucinated deps — better-auth 1.6.23 pinned (bun.lock) and its abort/`definePayload` contracts spiked empirically (`auth.config.ts:25-43`). Realistic failure handling: fail-closed session gate (`if (!user) return false`, `:199`), FK-existence filter before applying grants so a deleted-mid-window role can't roll back the activation tx and strand a now-active user's invitation (`:92-108`), P2002→409 race close. Security-conscious per the OWASP round: redacted `requestLogger` wired live (`index.ts:40`, drops the `?token=` query string), `Referrer-Policy: no-referrer` on both `/invite` routes, soft-delete `deletedAt→404` guards on the four sub-routes that lacked them, `NOTIFY_INTERNAL_TOKEN` min-32 (`env.ts:61-63`). |
| Design fidelity (UI) | 15 | 5 | 3 | 15.0 | `design.md` complete: 6 flows (F1..F5+F1a), every L/E/P/Err/Guardrail state per screen, a full a11y plan (tri-state select-all `indeterminate`, persistent `role=status` bulk-result panel — never a toast, focus-on-mount `role=alert` landing states, non-color `InvitationStatusBadge`, disambiguating per-row `aria-label`s, `motion-reduce` on the reused spinner), component inventory with reuse:NEW mapping (~6:10) and an explicit Gaps section routed to PO. Realised in code + tests: `InvitationsPage`, `InviteUserModal`, `InvitationStatusBadge` (glyph+text, 5 tests), `BulkDeleteResultPanel` (`role=status`, 5 tests), `UsersSubNav`, and the backward-compatible `ConfirmDeleteModal` `body` extension (12 tests, existing callers unaffected) — all within the 257 passing admin-ui tests. The hosted `auth`-side landing (Screen I1) matches `signin.routes.ts` conventions. |
| Trajectory | 10 | 2 | 2 | 4.0 | No `wellforge-run/v1` JSON trace for 006 in `.forge/runs/` (the `.events.jsonl` present is token telemetry only — `{ts,event,model,input_tokens,output_tokens}`, no spec/agent attribution), so per the rubric note this scores the neutral floor. Git history nonetheless corroborates a correct, ordered trajectory: docs commit (spec+plan+design+tasks+ADRs 0011-0013, `e62b58d`) → wave 1 T1-T6/T9 (`4feb893`) → wave 2 T7/T8 (`03077d7`) → admin-ui T10-T12 → infra T13 → e2e T14, via worktree-agent merges, then a QE-driven AC-2.3 fix (`b98777b`) and a frontier OWASP round (`e8cb3b5`, 1 HIGH + 3 MED + 2 LOW all fixed). An honest FAIL→fix→PASS loop, not the observability-trace evidence anchors 4-5 require. |
| **Total** | | | | **95/100** | (109 weighted ÷ 115 applicable weight × 100 = 94.8) |

**Verdict: PASS** — 95 ≥ pass_score 80; every applicable dimension ≥ its floor (ac 5≥4; spec/test/code/design 5≥3; trajectory 2≥2). No sub-floor dimension, no unmet AC.

## Findings
- **Trajectory (2/5)** — the only below-5 dimension, and not a defect in the work. No `wellforge-run/v1` run trace exists for feature 006 (`.forge/runs/` holds only per-feature traces for 001-003 plus 004/005 orchestrate traces and a token-telemetry `.events.jsonl`), so per the rubric's own note ("default to 2 (neutral) ONLY when no run trace exists") this is the neutral floor. Git history independently evidences a clean, ordered, QE-and-OWASP-reviewed trajectory. Nothing to remediate.
- **QE loop was real, not a rubber stamp.** The QE FAIL surfaced a genuine spec-fidelity defect (AC-2.3 baseline-vs-exact-roles, plan R9) and a security-composition class of bug; both were fixed and re-verified. The exact-roles resolution is confirmed in code (`auth.config.ts:113-124`, named-role invite deletes then re-creates from the invitation's valid ids) and asserted as an e2e control (`invitations.spec.ts:76`, active user holds only the invited role, no baseline `employee`).
- **Documented, PO-routed design gaps (non-blocking).** `design.md` §Gaps flags that AC-2.4's "wrong OAuth account" case has no UI surface (inherent to the plan's verified-email-match mechanism, mitigated by showing the expected email pre-sign-in) and that the landing page's bilingual scope is a design inference, not AC-locked. Both are surfaced for PO confirmation, not silently scoped — worth a one-line PO note, not a defect.

## Recommended next step
- PASS → spec 006 may complete T15 (`/wellforge:done 006`) and move to `done`.
