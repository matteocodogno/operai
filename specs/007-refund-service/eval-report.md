---
spec: 007
evaluated: 2026-07-16
rubric: default-v2
score: 95
verdict: PASS
---
# Eval report: Refund service (Rimborsi) — expense requests, expense lines & accounting review

| Dimension | Weight | Score (/5) | Floor | Weighted | Evidence |
|---|---|---|---|---|---|
| AC satisfaction | 35 | 5 | 4 | 35.0 | All 43 ACs (US-1..US-8, incl. amended AC-1.4/2.2 + AC-8.4) mapped in `tasks.md` AC-coverage matrix + `plan.md`'s total AC→test table, each to ≥1 passing test AND observable behavior. QE verdict PASS (42/42 mapped; refund-api 200 / refund-ui 257 / auth 314 / notify-api 122 green; T21 headline e2e `shell/e2e/refund-headline.spec.ts` live 2/2). Independently re-verified: ABAC predicate units 36/36 (`conditions.test.ts`+`s3Residency.test.ts`); `/authz/resolve` 9 adversarial cases incl. caller-own-only + wrong-aud/tampered-sig 401 (`auth/src/auth/authz-resolve.routes.test.ts:132,193,291`); notify seam token invariant (`notify-api/.../notifications.routes.test.ts:229`); the retain-once-submitted FK-restrict 409 (`requests.routes.test.ts:471`); decided-immutability 409s + whole-request decision (`decide.routes.test.ts`, 25 cases); audit immutability under direct SQL (`db.audit-immutability.test.ts`, 5 cases). |
| Spec fidelity | 20 | 5 | 3 | 20.0 | Implementation realizes plan/spec intent exactly: server-side ABAC via the NEW Bearer-authed `GET /authz/resolve` seam (ADR-0014), request-level "≥1 line matches caller.entity, unconditioned⇒global" scope via ONE shared `requestInScope` predicate backing both queue and record access (`conditions.ts:104`, `review.repo.ts` header — prevents queue/record desync), whole-request decisions incl. out-of-scope lines (`decide.routes.ts:287,365`, AC-7.6), integer-cents money with currency DERIVED from entity and never blended (`plan.md` §Money; `subtotals`), cross-user in-app notify seam (ADR-0017), immutable audit + `onDelete: Restrict` retention (ADR-0018), EU-region storage (ADR-0016). The delete-drift (AC-1.4/2.2/8.4 "retain once submitted") was resolved spec-first (`81ce97d`) then code aligned — correct drift discipline, not silent change. Zero unrequested product scope. |
| Test quality | 20 | 5 | 3 | 20.0 | Tests are adversarial and defeat vacuous passes. `/authz/resolve` asserts user A can NEVER retrieve user B's resolution (no id param exists) and rejects tampered-signature/wrong-audience tokens as controls. The notify seam asserts the ADR-0011/0017 invariant that a user JWT never substitutes for the internal token (`:229,:242`). The retain-once-submitted test drives the REAL submit/withdraw repo path then asserts the FK-restrict 409 (not the cheaper `status!=='draft'` branch), that both audit rows persist, and that the request stays editable (`requests.routes.test.ts:471-503`). Entity-scope predicate exhaustively unit-covered (single match/no-match, mixed ≥1, global, empty-lines). Live e2e 2/2 (approve + reject-with-motivation journeys). |
| Code quality | 15 | 5 | 3 | 15.0 | Idiomatic clone of `estimai-api`/`notify-api` (Effect TS, RFC 7807 `onError`/`notFound`, Prisma-only DB, zod boundary, `@hono/zod-openapi`/Scalar). No hallucinated deps. Fail-closed authz middleware — 503 on auth outage, never "allow", never serves a stale entry (`authz.middleware.ts:106-120`); fail-closed EU-residency guard verified firing at startup for a non-AWS endpoint without an operator allowlist (`env.ts:100-118`, `s3Residency.ts`). Defense-in-depth details: constant-time internal-token compare, per-path `bodyLimit` before auth (OWASP A04 fix `86fb92a`), notify best-effort double-catch that can never roll back a decision, `Symbol` global-scope sentinel, NUL cache-key separator. OWASP frontier pass 0 critical/0 high; 2 mediums (EU-residency fail-closed, bodyLimit) + an `owner.name` null-fallback defect fixed and merged. Minor, documented nits below. |
| Design fidelity (UI) | 15 | 5 | 3 | 15.0 | `design.md` complete: 7 flows (F1-F7), every screen state (L/E/P/Err/PD/NF/RO across R1/R2×4-variants/A1/A2×2/`RefundShell`), a full a11y plan (type-driven `km` live-announced appear/disappear, per-line approved-total row-identity `aria-label`s, disabled-until-valid required-motivation `RejectDialog`, per-file upload `aria-live` state machine, focus-on-transition, glyph+text+color badges), a component inventory with an explicit reuse:NEW ratio (~5:16) grounded in the Module-Federation port-not-import boundary, and a Gaps section routed to PO/architect rather than silently scoped. Realized across 33 refund-ui test files (pages + components); the `owner.name` null defect surfaced and fixed (`ab51a6f`). |
| Trajectory | 10 | 2 | 2 | 4.0 | No `wellforge-run/v1` trace exists for feature 007 (`.forge/runs/.events.jsonl` has 0 "007" entries; it is token telemetry only) → neutral floor per the rubric note ("default to 2 ONLY when no run trace exists"), consistent with the 005/006 evals. Git history nonetheless corroborates a clean, ordered trajectory: docs+ADRs 0014-0018 (`36f8fab`) → Wave-1 seams T1-T4/T14 → domain chains T5-T13 → UI T14-T18 via worktree-agent merges → an honest QE/OWASP FAIL→fix loop (delete-drift `81ce97d`, `owner.name` `ab51a6f`, 2 OWASP mediums `c9a3b05`/`86fb92a`, then T21 e2e `88c164c`). |
| **Total** | | | | **95/100** | (109 weighted ÷ 115 applicable weight × 100 = 94.8) |

**Verdict: PASS** — 95 ≥ pass_score 80; every applicable dimension ≥ its floor (ac 5≥4; spec/test/code/design 5≥3; trajectory 2≥2). No sub-floor dimension, no unmet AC.

## Findings
- **Trajectory (2/5)** — the only below-5 dimension, and not a defect in the work. No run trace exists for 007, so per the rubric's own note this scores the neutral floor; git history independently evidences a correct, ordered, QE-and-OWASP-reviewed trajectory with a genuine FAIL→fix→PASS loop. Nothing to remediate.
- **i18n English-only for v1 (deliberately deferred, acceptable).** CLAUDE.md mandates "i18n from day one," and `design.md` Gaps #1 flagged that no plan.md task selects a mechanism. The Gate decision shipped English-only (`tasks.md:16`). This is a documented, PO-routed deferral, not silent drift — and the "no hardcoded strings" half of the mandate IS honored (copy centralized in `refund-ui/src/strings.ts` + the `EXPENSE_TYPES` dictionary), so only the bilingual population is outstanding, a low-debt later swap. Acceptable for this tier; worth a one-line PO confirmation that IT copy is a fast-follow, not forgotten.
- **Shared `NOTIFY_INTERNAL_TOKEN` (accepted trade-off).** Three services now share one internal secret (auth/refund-api/notify-api), with a scoped self-issued service JWT named as future hardening (ADR-0017 §, mirrors ADR-0011). OWASP frontier pass returned 0 critical/0 high and the route-exclusivity invariant is test-enforced (JWT never substitutes for the token). Acceptable for an internal-only, private-networking call at this tier.
- **No in-app audit-trail viewer (correct non-goal).** `design.md` Gaps #2: US-8's ACs are entirely about the record being captured and immutable, not browsable; adding an `AuditPage` analog would be scope creep against the AC set. Correctly deferred.
- **Minor, non-blocking code nits (not score-affecting):** (1) the `(sub, perm_epoch)` authz cache `Map` is unbounded — entries expire by TTL check but are never evicted, a slow stale-key accumulation harmless at this tool's scale; (2) the review queue fetches ALL `submitted` requests then filters in-app (`review.repo.ts:40`) with no pagination — a latent scale gap already flagged to architect in `design.md` Gaps #6, correct for now but worth revisiting if the company-wide queue grows large.

## Recommended next step
- PASS → spec 007 may proceed to T22 (`/wellforge:done 007-refund-service`) and move to `done`. Carry the i18n English-only deferral and the shared-token → service-JWT hardening forward as tracked follow-ups, not blockers.
