---
spec: 003
evaluated: 2026-07-10
rubric: default-v2
score: 90
verdict: PASS
---
# Eval report: Operai suite shell (Module Federation)

| Dimension | Weight | Score (/5) | Floor | Weighted | Evidence |
|---|---|---|---|---|---|
| AC satisfaction | 35 | 4 | 4 | 28.0 | 24/25 ACs backed by passing test + observable behavior (shell e2e 30/30 fresh vs live stack; shell unit 71/71; estimai-ui 81/81; refund-ui 5/5). AC-1.2 chrome-persistence proven by DOM node-identity (`chrome-and-design.spec.ts:18` external-attr survival + `__e2eNoReload`); AC-2.4 sign-out proven server-side (`sign-out.spec.ts:39` re-presents raw cookie → null session); AC-2.2/AC-2.1/AC-2.5, AC-3.2/3.3, AC-3.4 (`router.root-redirect.test.tsx` 7 tests), AC-5.2, AC-6.1 (`remote-failure.spec.ts:46`) all exercised. **Thin (config+reasoning, no live behavior):** AC-4.3 (`estimai-ui/vercel.json` sec-fetch-dest redirect) and AC-5.3 (independent redeploy) are deploy-dependent, not Vercel-verified — honestly documented in `infra/vercel-deploy-runbook.md:13-17`. Every AC met; two lack observable behavior → anchor 4. |
| Spec fidelity / no drift | 20 | 4 | 3 | 16.0 | Zero unrequested scope; topology, chrome-ownership (AC-4.2), and federation contract match `plan.md`. ADR-0006 authored as required. Drift surfaced not buried: T18 retired estimai-ui standalone Playwright (a documented deviation from plan R4's "standalone suite stays the regression gate") because T13/T14 made estimai-ui a remote whose `import('shell/session')` can't resolve standalone — coverage relocated to `shell/e2e`, nothing dropped (`tasks.md:83` DRIFT note; trace 08-15-18Z). Documented, justified, harmless deviation → anchor 4. |
| Test quality | 20 | 5 | 3 | 20.0 | Non-tautological and regression-catching: chrome persistence asserts DOM identity via React-unmanaged attributes; sign-out asserts server-side session-row deletion (not just cookie); `estimai-identity-jwks.spec.ts` proves user A/B isolation + RFC7807 401 + real JWKS `kid`; import journey covers accept/partial-oversize/decline; `remote-failure` covers in-place error + retry + chrome-stays-usable. Edge/error cases genuinely exercised. (No coverage threshold configured repo-wide — assessed on quality; QE confirms.) |
| Code quality & conventions | 15 | 5 | 3 | 15.0 | Idiomatic; no hallucinated APIs/deps. `RemoteMount.tsx` hand-rolls the error boundary per the repo's no-extra-deps posture, handles network + MF-runtime + module-reject failures, and *honestly documents* the `React.lazy` retry limitation (browser-cached failed import → recovery via reload) with a justified `eslint-disable`. `ShellLayout.tsx` uses correct landmarks + skip link. Session/token handling preserves ADR-0001 in-memory invariant. |
| Design fidelity (UI) | 15 | 5 | 3 | 15.0 | `design.md` present → applies. Complete: 8 flows, every loading/loaded/failed/guard-pending state, full a11y plan (landmarks, skip-to-content, roving-tabindex + arrow keys, `aria-current`, focus-management on switch, `aria-live` polite loading, `role="alert"` errors, focus-visible rings, contrast), 8-reuse:5-NEW component inventory mapped to the codebase (no new UI lib). Realised faithfully — `ShellLayout` landmarks + `tabIndex=-1` skip target confirmed; `RemoteMount` sr-only `aria-live` loading + `role="alert"` retry confirmed; Sidebar keyboard nav in shell unit suite. |
| Trajectory | 10 | 5 | 2 | 10.0 | Full `.forge/runs/*implement-003*` traces: right agents (frontend-dev, quality-engineer, devops, owasp-reviewer) ran the right tasks in dependency order; **QE PASS round 1 on every slice** (T13/T15, T16, T17); T13/T15 ran in isolated worktrees, merged clean; drift events surfaced and resolved/correctly-deferred; a real bug (missing `filename:remoteEntry.js`) found+fixed by QE (f73eab6); owasp-reviewer PASS (0 medium+, `owasp-report.md`). Verification not skipped. |
| **Total** | 115 | | | **104.0 → 90/100** | normalised over applicable weights (design_fidelity applies) |

**Verdict: PASS** — 90/100 ≥ pass_score 80, and every applicable dimension ≥ its floor (ac_satisfaction 4 = floor 4; all others above). No sub-floor dimension.

## Findings
- **AC satisfaction (4/5):** the only reason this isn't a 5 — AC-4.3 (former standalone EstimAI URL redirects into the shell) and AC-5.3 (redeploy refund-ui without rebuilding shell/EstimAI) rest on Vercel `vercel.json` config + runtime remote-URL wiring that could not be exercised against a live Vercel deploy (no access). Evidence is authored config + structural reasoning, not observable behavior. This is documented transparently in `infra/vercel-deploy-runbook.md` (lines 13-17) and T17's trace, so it is a known pending human/deploy action, not a hidden gap. To reach 5: verify the redirect and independent-redeploy on a real preview deploy. Not blocking — every AC is met and the floor (4) is satisfied.
- **Spec fidelity (4/5):** the retirement of the estimai-ui standalone Playwright suite deviates from plan R4's stated mitigation. Correctly reconciled (journeys relocated to `shell/e2e`, estimai-ui unit 81/81 is now its standalone gate, nothing dropped) and fully documented, so it is a harmless documented drift — but it is a deviation from the approved test strategy, hence 4 not 5.
- **Non-blocking honest limitations (weighed, not penalised beyond the above):** `RemoteMount` retry cannot re-fetch a browser-cached failed `import()` (recovery via reload) — documented + tested to the achievable bound; the `*.vercel.app` preview-URL CSP gap fails *closed* (documented follow-up, not an exposure).

## Recommended next step
- **PASS** → spec 003 may move to `done`. T20 (`/wellforge:done`) can proceed: QE PASS on every slice, owasp PASS, and this fresh eval PASS. Recommend the team close the two live-deploy verifications (AC-4.3 redirect, AC-5.3 independent redeploy) as the documented post-deploy checklist in `infra/vercel-deploy-runbook.md` when Vercel access lands — they are the only evidence gaps, and both fail closed.
